export default class Modal {
  constructo({ modalSelector}) {
   this._modalElement = document.querySelector(modalSelector);
  }

  open() {
    // opens modal
  }

  close() {
    // closes modal
  }

  _handleEscClose() {
    // listens for esc button
  }

  setEventListeners() {
    // sets event listeners
  }

}